# Handoff: CCC-IMA Process Mining Explorer — Round 4+ Polish

## Mission

Continue iterating the CCC-IMA clinical process mining demo toward stakeholder-meeting quality (target 8/10). Run generator-evaluator loops: fix issues → screenshot with agent-browser → grade → fix → repeat. The app is a Celonis-style process mining visualization built on a clinical workflow discrete event simulation.

## Context Files (read in order)

1. `.claude/CLAUDE.md` — Project overview, tech stack, commands
2. `.claude/evaluation-round1.md` — Round 1 evaluation (4.5/10), issues #6 and #10 still open
3. `src/routes/index.tsx` — Main narrative landing page (5-question story flow)
4. `src/components/process-graph/ActivityNode.tsx` — Rectangular phase-colored nodes
5. `src/components/process-graph/ProcessEdge.tsx` — Orthogonal edges with frequency scaling
6. `src/components/process-graph/ProcessGraph.tsx` — ELK layout + React Flow integration
7. `src/components/simulation/Timeline.tsx` — Gantt chart (missing legend)
8. `src/components/simulation/MonteCarloCharts.tsx` — 10 charts (stacked bars issue)
9. `src/components/variant-panel/VariantList.tsx` — Color-coded pills, progressive disclosure
10. `src/mining/enriched-event-log.ts` — Generates 20+ activities with branching by case type

## Celonis Reference Screenshots

Study EVERY image in `~/Desktop/celonis/` before making changes. Key observations:
- **New Studio Views**: Subway-map orthogonal routing, thick colored lines (purple/teal/orange), activity labels as station-style rectangles
- **Alignment Explorer**: Left panel with variant list + coverage %, right panel with target model graph, throughput time popups
- **Process Cockpit**: Benchmarking with side-by-side process comparison, big KPI numbers
- **Classic Analysis**: Circular nodes with dashed blue edges (deprecated but recognizable)
- **Conformance Overview**: KPI cards for conforming %, violations list with impact metrics

## Verified State (don't re-derive)

- 17 commits on `claude/process-mining-explorer-BbJim`, all pushed
- 66 tests passing, TypeScript clean, Vite build succeeds
- 6,183 lines TypeScript across all src/ files
- Auto-runs simulation + 5000 MC on page load
- 500 mining batch runs = 4,000 patient encounters, 67 unique pathways
- Graph shows 35 nodes with orthogonal edges, color-coded by clinical phase
- Data persists across tabs via useEnsureSimulation hook

## Open Issues

### From Round 1 Evaluator (must fix)
- **#6**: Timeline Gantt has NO COLOR LEGEND — 10+ colors never explained. Add a compact legend bar.
- **#10**: Stacked bar charts (Patients Seen by CT/Attending) unreadable — opacity stacking produces indistinguishable segments. Switch to grouped bars or distinct colors.

### Visual Polish (should fix)
- Process graph default zoom is too zoomed out — labels hard to read without manual zoom
- Edge frequency labels could be more prominent on high-frequency edges
- The "What should we do next?" section feels generic — should reference actual computed metrics
- No loading skeleton/shimmer during data generation (just a spinner)
- The Variant Explorer histogram bars could be wider
- Node hover states could show more detail (tooltip with throughput time, case count breakdown)

### Celonis Parity Gaps (nice to have)
- No conformance checking view (compare actual vs target process model)
- No throughput time overlay on edges (Celonis shows "11 hours" on edges)
- No filter panel on right side (Celonis has filter dropdowns)
- No animation (pink bubbles were in original spec but never implemented)

## First 3 Tasks

1. **Fix #6 + #10**: Add timeline legend, fix stacked bar charts. Quick wins. ~30 min.
2. **Run evaluator**: Start dev server, use agent-browser to screenshot every section, write evaluation to `.claude/evaluation-round2.md`. Grade each section 1-10.
3. **Fix top evaluator findings**: Address the highest-impact issues from the evaluation.

Then repeat steps 2-3 until score >= 8/10.

## Generator-Evaluator Protocol

### Evaluator Phase
```bash
pnpm dev --port 5185 &
sleep 3
agent-browser open http://localhost:5185/ccc-ima-sim/
agent-browser eval 'localStorage.setItem("ccc-ima-guide-dismissed", "true"); location.reload()'
agent-browser wait 18000  # wait for MC to complete
# Screenshot each section by scrolling main container:
agent-browser eval 'document.querySelector("main").scrollTop = N'
agent-browser screenshot /tmp/eval-section-N.png
# Read each screenshot and grade
```

### Creator Phase
- Fix issues identified by evaluator
- `npx tsc --noEmit` + `pnpm test` before every commit
- `git add -A && git commit -m "..." && git push origin claude/process-mining-explorer-BbJim`

## Constraints

- Do NOT change simulation core logic (src/core/) — it's tested and stable
- Do NOT change test files unless adding new tests
- Do NOT delete the legacy/ directory
- Do NOT push to main/master
- Keep code LEAN — the user cares about LOC efficiency
- Use `pnpm` for everything (not npm)
- Commit after every meaningful change, push after every round

## Seance Protocol

If you need information not in this handoff:

**Shared file:** `.handoff/seance-round4.md`

To ask a question:
1. Write to the seance file: `echo "Q: your question" >> .handoff/seance-round4.md`
2. The user will see it and respond

**What the original session established:**
- The original 2017 presentation narrative: "Should we add clinical teams?" → Alice says yes, Bob says no → simulation reveals attending is the bottleneck
- The sales pitch: process mining reveals hidden complexity in what looks like a simple 5-step process
- Monte Carlo is framed as synthetic data generation, not a separate analytics tool
- The enriched event log decomposes broad activities into sub-steps by case type (UC/BTC_NEW/BTC_FU) with optional activities (labs, imaging, referrals) — see src/mining/enriched-event-log.ts
